# React hooks

Hooks connect React lifecycle to workers, background fetches, polling, board
ownership, and dialogs. A hook should expose a small state/action contract to a
view and keep timers, cancellation, and stale-response handling out of JSX.
