# Frontend utilities

Small reusable helpers for chess notation and FEN, cards, dates, settings,
local-runtime detection, annotations, tablebases, URLs, and PGN formatting.
Utilities should remain side-effect-light; move API calls into typed adapters or
hooks when a helper starts owning a workflow.
