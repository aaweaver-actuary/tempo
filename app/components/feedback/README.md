# Feedback components

`FeedbackIcon` and `FeedbackText` render the shared correct/again feedback
language. Keep outcome interpretation in the training domain or owning view;
these components should remain presentational.
