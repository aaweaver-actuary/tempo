# Sites hosting metadata

This directory contains local Sites hosting configuration. It is deployment
metadata, not application behavior; changes should be reviewed with the hosting
workflow and the static-build assumptions in `static/README.md`.
