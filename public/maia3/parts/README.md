# Maia model parts

The model is stored in numbered parts so it can be served and loaded without a
single oversized asset. Keep part names and ordering aligned with the loader;
these files are generated/runtime assets, not application modules.
