# Public runtime assets

Files here are served by the web application and static build: packaged tactic
data, local engine/ML runtime assets, chess pieces, sounds, and icons.

These are deployable assets rather than application logic. Update the relevant
subdirectory README and asset-validation tests when replacing or regenerating
them. Missing assets must produce actionable errors, not demo-data success.
