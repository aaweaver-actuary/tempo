# Domain model alignment review

The `SKILL.md` in this directory describes a focused review of frontend/backend
domain contracts. Use it when transport schemas, adapters, persistence models,
or Rust parity boundaries change.
