# Visual baselines

PNG baselines are reviewed artifacts for the pinned browser/viewport matrix.
They are not source screenshots to edit casually. Run `npm run test:visual` to
compare; pass the explicit update flag only when preparing a candidate for
review. CI must not accept screenshots automatically.
